"""In-process pub/sub for layer-to-layer signals.

Phase 1.0: empty placeholder.
Future phases may emit events for: filesystem changes, Postgres
reconciliation results, tree rebuilds. The mechanism is in-process
only — no external bus.
"""